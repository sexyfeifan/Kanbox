[package]
name = "kanbox"
version = "0.0.1"
description = "Local desktop shell for Kanbox"
authors = ["sexyfeifan"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
