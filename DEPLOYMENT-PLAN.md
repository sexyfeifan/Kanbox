# Kanbox 部署与打包现状

## v0.8.11 同步冲突处理验收

- 同修订分叉合并产生的 `syncConflict` 与字段列表必须完整传到前端，不得在标准化时丢失。
- 首页提供同步冲突智能视图与数量；无冲突资料库显示 0，有冲突时可一键筛出全部待审核笔记。
- 阅读器必须展示冲突字段和明确的人工确认操作，不允许后台静默清除冲突。
- 确认当前内容必须原子清除冲突标记、推进修订号并写入当前设备 ID；笔记正文、媒体和人工整理结果保持不变。
- 收藏、阅读状态、最近阅读时间以及同步修订元数据在 API 标准化后必须完整保留。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.11 发布门槛。

## v0.8.10 批量导入验收

- 粘贴导入必须先规整输入，并按小红书笔记 ID 去重；不同查询参数指向同一笔记时只处理一次。
- 单次原始粘贴最多接收 500 行，去重后的有效候选最多 50 条；超过限制时在写入前明确拒绝。
- 重复项必须单独报告为跳过，不能计入失败；无效文本作为对应行失败，不影响其他有效项。
- 导入完成后必须分别报告新增、更新、失败和重复跳过数量，并保留失败输入以便一键重试。
- 扩展使用的结构化 `items` 协议必须保持兼容；批量准备维持最多 3 路并发，并在准备完成后单次原子写入成功项。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.10 发布门槛。

## v0.8.9 标签与批量整理验收

- 批量模式支持为选中笔记添加/移除多个标签、统一分类，并可全选当前筛选结果。
- 标签必须规整空白、去重并限制单条长度与每篇数量；同一标签同时添加和移除时以移除为准。
- 批量整理必须先验证全部笔记 ID，任何无效或不存在的 ID 都整批拒绝且不允许部分写入。
- 批量整理单次原子写回，每条修改均推进修订号和设备标识，并通过 SSE 广播实时刷新。
- 批量删除单次写回笔记集合，为所有删除项一次性记录同步墓碑；媒体清理采用独立容错。
- 端到端测试覆盖添加/移除冲突、不存在 ID 回滚、批量删除、归档恢复和墓碑防复活。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.9 发布门槛。

## v0.8.8 智能视图与阅读队列验收

- 搜索支持标题、作者、标签、分类、OCR 和视频文稿字段限定，支持精确短语与排除词组合；普通搜索、拼音和模糊匹配不得回退。
- 首页提供全部、收藏、未读、稍后阅读、已读、近 7 天读过和今日收录七个智能视图，并显示实时数量。
- 智能视图必须能与全文搜索、分类筛选和分组视图组合，不改变或复制原始笔记数据。
- 最近阅读仅接受过去 7 天内的有效时间；今日收录按设备本地日期判断，旧笔记缺字段时仍兼容为未读。
- 排序新增最近阅读时间，未读笔记置后并以收藏时间稳定兜底；现有最新、最早和标题排序保持不变。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.8 发布门槛。

## v0.8.7 收藏与稍后阅读验收

- 单条笔记支持收藏、未读、已读、稍后阅读和最近阅读时间，旧笔记无字段时兼容为未读。
- 卡片和阅读器提供快捷操作，打开详情自动标为已读；首页支持收藏、未读和稍后阅读筛选。
- 批量模式支持批量收藏、稍后阅读与标为已读，非法 ID 和空操作必须拒绝。
- 每日回顾优先稍后阅读与未读内容，回顾动作同步笔记状态；重导入不能清除人工状态。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.7 发布门槛。

## v0.8.6 每日回顾正式版验收

- 首页每日回顾入口可见，资料为空时提供明确提示，非空资料可读取稳定的当日列表。
- 支持历史上的今天、长期未回顾优先、每日数量设置、已回顾、稍后复习、重置和连续完成统计。
- 所有进度写入 `daily-review.json`，损坏文件保留副本后安全重建，写入使用临时文件原子替换。
- 回顾记录必须跟随资料库迁移，并进入 JSON 备份和完整 `.kanbox` 归档；旧归档仍可兼容恢复。
- 单元测试、本地 API 端到端测试、lint、TypeScript 和生产构建全部通过。
- Apple Silicon 与 Intel 安装包构建、校验并上传 GitHub Release；远端摘要与本地 SHA256SUMS 一致。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.6 发布门槛。

## v0.8.5 资料库发现与恢复验收

- 只扫描 Kanbox 已知目录、存储历史、迁移快照和本地备份，不递归搜索用户主目录。
- 候选必须报告笔记/媒体数量、体积、更新时间与健康状态；损坏候选不可直接恢复。
- 恢复前必须展示合并后的新增、更新、保留、冲突与跳过数量，完整归档先校验清单与 SHA-256。
- 当前资料库为空且存在历史候选时主动打开恢复区域，不允许空库静默覆盖非空旧库。
- 恢复非空资料库前创建完整安全归档；目录恢复保留源目录及当前资料库快照。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.5 发布门槛。

## v0.8.4 存储迁移验收

- 存储切换必须以当前活动资料库为源，不得固定使用本机默认目录。
- 本机、iCloud、自定义目录的七种方向组合均由自动化测试覆盖。
- 目标已有资料时执行笔记、工作区、删除墓碑、媒体与备份的并集合并；当前资料库设置优先。
- 迁移先写目标旁暂存目录，校验成功后原子提交；源目录永不删除，旧目标保留为 `.kanbox-before-migration-*` 快照。
- 迁移中断保留 `.kanbox-migration-in-progress.json`，目标和源资料不变；重新执行可安全恢复。
- Developer ID 公证和 Chrome Web Store 正式发布继续暂缓，不作为 v0.8.4 发布门槛。

## 当前形态

- Tauri 桌面应用（macOS 13+，Apple Silicon / Intel）
- Next.js 首页整理台
- Node 本地 sidecar
- 安装包内置 Node 运行时
- Chrome Quick Import 扩展
- 本地 `notes.json` + `workspace.json` 数据存储（支持 iCloud / 本机默认 / 自定义三种位置）

## 打包命令

```bash
npm run tauri:build
```

## 浏览器扩展

开发阶段从 `browser-extension` 加载已解压扩展。正式分发前需要单独发布或打包扩展；Tauri 不会自动替用户安装 Chrome 扩展。

## 当前数据边界

- 扩展只读取拖动卡片已有的链接和标题，不具备 `tabs` 或 `cookies` 权限
- Sidecar 不带 Cookie 匿名解析单篇公开笔记页面，保存配图并调用 macOS Vision OCR
- 视频保存到本地，由 macOS Speech 分段离线转写
- App 不保存小红书 Cookie
- App 不调用收藏夹或评论接口，也不回退到 Chrome 登录态
- App 不做定时抓取；批量导入仅处理用户主动粘贴的最多 50 条单篇公开笔记链接
- 默认不联网做 AI；仅当用户主动填 API key 开启在线摘要/拓展/转写增强时，才单条、按需发往用户指定的服务商

## 当前风险

- 小红书页面 DOM 变化时，扩展选择器可能需要更新
- 匿名页面拒绝访问或结构变化时，导入会失败，但不会改用账号登录态
- 小红书公开页面状态结构变化时，正文和完整配图解析规则可能需要同步更新
- 当前使用 ad-hoc 签名，未公证版本首次运行需要用户在”隐私与安全性”中点”仍要打开”
